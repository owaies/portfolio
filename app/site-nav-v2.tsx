"use client"
import { AnimatePresence, motion } from "framer-motion"
import { ArrowUpRight, Menu, X } from "lucide-react"
import { useEffect, useState } from "react"
const links=["about","skills","projects","experience","education","certifications","resume","contact"]
export default function SiteNavV2(){const [open,setOpen]=useState(false);const [scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>24);addEventListener("scroll",f,{passive:true});return()=>removeEventListener("scroll",f)},[]);useEffect(()=>{document.body.style.overflow=open?"hidden":"";return()=>{document.body.style.overflow=""}},[open]);return <header className={`cinematic-nav ${scrolled?"is-scrolled":""}`}><div className="container nav-inner"><a href="#home" className="nav-brand">MO<span>.</span></a><nav className="nav-links" aria-label="Primary navigation">{links.map((x,i)=><a key={x} href={`#${x}`}>{x[0].toUpperCase()+x.slice(1)}<i/></a>)}</nav><a href="#contact" className="nav-cta">Get in touch <ArrowUpRight size={15}/></a><button className="nav-menu" aria-label="Open menu" aria-expanded={open} onClick={()=>setOpen(true)}><Menu/></button></div><AnimatePresence>{open&&<><motion.button className="nav-overlay" aria-label="Close menu" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setOpen(false)}/><motion.aside className="nav-drawer" initial={{x:"100%"}} animate={{x:0}} exit={{x:"100%"}} transition={{duration:.35,ease:[.22,1,.36,1]}}><div className="flex items-center justify-between"><span className="nav-brand">MO<span>.</span></span><button className="nav-close" aria-label="Close menu" onClick={()=>setOpen(false)}><X/></button></div><nav className="mt-10 grid">{links.map((x,i)=><a key={x} href={`#${x}`} onClick={()=>setOpen(false)} style={{animationDelay:`${i*45}ms`}}>{String(i+1).padStart(2,"0")}<span>{x[0].toUpperCase()+x.slice(1)}</span><ArrowUpRight size={17}/></a>)}</nav><a href="#contact" onClick={()=>setOpen(false)} className="primary-cta mt-auto">Start a conversation <ArrowUpRight size={16}/></a></motion.aside></>}</AnimatePresence></header>}
