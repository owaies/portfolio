# Accessibility Testing

Keyboard-test every changed interactive path: navigation, dialogs, forms, buttons, and links.

Confirm focus is visible, focus is not trapped outside an open dialog, Escape closes dismissible overlays, and status/error messages are understandable without relying on color alone.

Test narrow viewports and browser zoom because responsive layout failures often become accessibility failures when controls overlap or become unreachable.
