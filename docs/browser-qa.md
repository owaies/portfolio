# Browser QA Matrix

For meaningful UI changes, smoke-test the public portfolio in a Chromium-based browser and at least one additional browser engine when available.

Check navigation, scrolling, forms, external links, project detail routes, responsive layout, and error/loading states.

Pay particular attention to sticky navigation, dialogs, focus restoration, and media sizing because these areas behave differently across viewport and browser implementations.
