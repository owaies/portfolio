# Dependency Policy

Prefer the existing Next.js, React, Tailwind, Lucide, and Supabase stack when a new capability can be implemented without another package.

Review dependency updates for compatibility and security impact before merging. Keep the lockfile synchronized with package manifest changes.

A small feature should not introduce a large dependency surface without a clear maintenance or performance benefit.
