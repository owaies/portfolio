# Loading and Error UX

The App Router uses dedicated loading and recovery surfaces so slow data access and unexpected failures remain understandable.

Loading states should communicate what is being prepared without blocking assistive technology users from receiving status updates.

Error states should provide a clear recovery action and a safe route back to the public portfolio.

When adding animation to skeletons or recovery screens, preserve the existing reduced-motion behavior.
